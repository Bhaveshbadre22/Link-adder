# Link Storer

Simple local web app to store links with previews and folders.

Prerequisites
- Node.js (14+)

Install

```bash
npm install
```

Run

```bash
npm start
# open http://localhost:3000
```
